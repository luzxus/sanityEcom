import banner from "./banner";
import product from "./product";
import image from "./image";
import tags from "./tags";
import category from "./category";
export const schema = {
  types: [product, banner, image, tags, category],
};
